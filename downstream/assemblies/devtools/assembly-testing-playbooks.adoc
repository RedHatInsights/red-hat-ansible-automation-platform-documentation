ifdef::context[:parent-context: {context}]
[id="testing-playbook-project"]

// Used in /titles/develop-automation-content/
//
= Testing a playbook project

:context: testing-playbook-project
[role="_abstract"]

You can use `molecule` to test your playbooks. Create a separate `molecule` scenario for every playbook in your project.

include::devtools/proc-create-molecule-scenario.adoc[leveloffset=+1]

ifdef::parent-context[:context: {parent-context}]
ifndef::parent-context[:!context:]

